name: Jira Smart Commit

on:
  push:
    branches: [main, master]
  pull_request:
    types: [opened, closed]
    branches: [main, master]

jobs:
  jira:
    uses: Khanna-Consulting/.github/.github/workflows/jira-smart-commit.yml@main
    secrets: inherit
